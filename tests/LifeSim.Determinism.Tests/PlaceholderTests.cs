namespace LifeSim.Determinism.Tests;

/// <summary>
/// Placeholder so the suite builds and runs green. The two flagship determinism tests —
/// Seed Replay and Save/Reload Equivalence — arrive in Phase 4 once the tick loop exists.
/// See tasks.md Phase 4 and lifesim.md §15.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Scaffolding_buildsAndRuns()
    {
        Assert.True(true);
    }
}
